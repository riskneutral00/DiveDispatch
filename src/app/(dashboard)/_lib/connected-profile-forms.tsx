/**
 * Re-exports — canonical implementation lives in `@/lib/profile/connected-role-forms`
 * so `components/` can import connected forms without depending on `app/`.
 */
export { RoleProfileForm, WorkspaceEmbeddedProfileForm } from '@/lib/profile/connected-role-forms'
