// ── Form Configuration Constants ────────────────────────────────────────────
// Named constants extracted from inline magic numbers across form components.

/** Maximum number of days a course can span (used in course-duration inputs). */
export const MAX_COURSE_DAYS = 14

/** Maximum post-job block duration in minutes (used in preferences editor). */
export const MAX_SESSION_MINUTES = 480

/** Default row count for multi-line textarea fields. */
export const DEFAULT_TEXTAREA_ROWS = 3

/** Maximum number of search results shown in dropdown lists. */
export const MAX_SEARCH_RESULTS = 6
