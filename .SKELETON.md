# DiveDispatch — Launch Skeleton

> **Goal:** MVP production launch
> **Last updated:** 2026-04-22
> **Current position (2026-04-22):** **Wave 3 closed end-to-end on main.** W3-A Steps 4/5/12 + W3-B pill-reactivity fix — 4 commits directly on main (4086e3de, 7888b40a, e0255751, 856a6117). Step 4: `validateContactInput` asserts phone E.164 + address.country + language codes; wired at 6 mutation boundaries; new PostToolUse hook `convex-input-validator-guard.sh`. Step 5: `profileUpdate`'s 4th arg required (type-level correctness); fixed wrong-literal at `diveStaff.update`; `setRoleProfileComplete` fanned through 7 equipmentInventory + 3 venues mutations. Step 12: 6th+7th safeDbOps sites wrapped; `parseNumber` DRY migration; `demoBookings` v.any()→concrete validators with `Infer<typeof>` array types + new `isOperatorRole` type predicate; new checkpoint-commit-blocker hook; new migration-manifest rule; two new Lessons.md entries. W3-B: root cause is Convex client subscription layer (convex-test PASSES — proves backend reactivity is correct; live browser occasionally misses invalidations). Fix: belt-and-suspenders second reactivity path via already-queried `myRoles` denorm (Step 5 wired the fan-out that makes this reliable). `TopNav` short-circuits pill + banner on `roleComplete`. Tests **5246/5247** (+5 W3 new, 1 pre-existing flake). tsc clean. /gate --all CLEAN: 0 CRITICAL, 2 HIGH both dismissed with full cited reasoning (DD-512 PRD-prescribed, DD-513 undoes W3-B by design). Two hook registrations pending manual user action (agent self-modification blocked). Terminal on main, 15 commits ahead of origin. **Next:** browser sign-off on venue Phase B per Phase 3 plan, then resume Rene → Pool role capture. Prior (2026-04-21 evening): **Venue multi-row migration + EntityCardList primitive + equipmentInventory orphan FK sweep + gear-matrix pending-race fix shipped.** Pool + DiveSite → Venue collapse completed end-to-end (schema, mutations, inventoryUnits rekey'd on venue.slug, frontend UI via new EntityCardList primitive with DiveCenterAccessPicker, Contact tab repointed to organizations, VenueBasicStep in onboarding). Equipment bulk-save crash fixed via `convex/lib/safeDbOps.ts` (6 sites). First-save disappearance race fixed via optimistic-manufacturer tagging on pending matrix. Tests 5249/5249. 5 commits landed (aeb36cde, f69ff783, ae33d8bb, 61fab388, 07d1ed21). /gate CLEAN. Earlier today: **Theme catalog prod-bootstrap closed.** `convex/lib/ensureSystemThemes.ts` materialized via `createUser` + `upsertFromWebhook` — dev and prod share one code path, no seed dependency. Pattern `code-defined-system-rows` generalizes (future: courses, agencies, system tags). Tests 5249/5249 (+4 new). Also resolved same-day: **userRoles/org three-layer invariant** (schema required + `insertUserRole` required-param + `setUserOrganization` sync-writer; commit `ca78367b`). Yesterday: **Clerk/Convex org-sync hardened.** /gate on 40-file Wave 8 reversal diff (webhook-driven Clerk-backed orgs + Convex-synthesized personal orgs) returned 1 CRITICAL + 4 HIGH, all resolved in one /gate cycle: (C1/H4) issuer-gated `tokenIdentifier` rebind in `users.upsertFromWebhook` + new `webhookAuditLog` table + `isAllowedRebind` helper; (H1) `cascadeOrgDelete` in `organizations.deleteFromWebhook` with grandchildren handling (cabins/trips/rooms) + new `by_organizationId` indexes on users + userRoles; (H2) `insertUserRole` helper populating `organizationId` at 4 insert sites; (H3) `isPersonalOrg` guard on `getActiveOrg` / `tryGetActiveOrg` fallback. Docs: new entity `clerk-convex-org-sync.md`, rewrote `auth-model.md` Rule 11 + added Rule 13, updated `organization.md` for two-kind (Clerk-backed + personal) reality. Second /gate flagged 3 more HIGHs: cascade grandchildren untested (AUTO-fixed) + 2 pre-existing Invariant-4 component test gaps (portal-submit + connected-equipment-gear — Matt chose "add tests now", Opus agent delivered 10 passing tests). Clerk dev instance wiped via new `scripts/wipe-clerk.ts`. Test baseline **5224/5224** (+19). tsc clean. /gate CLEAN. **Next:** Matt-driven Phase 3 per-user reseed ("recreate X in UI" → Playwright signup → capture-role-to-seed → commit → next). Prior: happy-path reseed Phase 1+2 shipped earlier today. Wave 8 Phase 3 signup CLOSED same day. Wave 5 closed same day. Wave 4 closed 2026-04-19.

---

## Launch Readiness Checklist

| # | Gate | Status | Notes |
|---|------|--------|-------|
| 1 | Security: input sanitization | **DONE** | DD-069 resolved. |
| 2 | Security: rate limiting | **DONE** | DD-081 resolved. |
| 3 | E2E: edit + cancel flows | **DONE** | DD-019 resolved. |
| 4 | E2E: happy path walkthrough | **DONE** | DD-094 resolved. |
| 5 | Frontend: enhanced directory | **DONE** | DD-058 resolved. |
| 6 | Backend test coverage ≥ 77% | **DONE** | 5246/5247 passing post-W3 (+ W3 lock tests: 18 i18nValidators + 6 venues + 2 portalCustomerMutations + 3 createUser + 1 equipmentInventory denorm + 8 parseNumber contract + 3 demoBookings + 4 top-nav-pill reactivity; 1 pre-existing flake on connected-equipment-gear mask-removal). Baseline returns to 5265+ as users are restored role-by-role via UI signup + capture-role-to-seed. |
| 7 | Frontend test coverage ≥ 50% | **DROPPED** | E2E coverage sufficient. Decision 2026-03-30. |
| 8 | Deploy config (Vercel + Convex prod) | **NOT DONE** | DD-214 → black_hole (was: Convex deploy in CI + `CONVEX_DEPLOY_KEY`). |
| 9 | Sentry integration | **NOT DONE** | DD-015 (P1, ready). Zero prod error visibility. |
| 10 | Screenshot-ready for ASPN deck | **NOT DONE** | 3 views needed: dashboard, resource provider, customer portal. |

---

## Ticket Summary

| Status | Count | Details |
|--------|-------|---------|
| Done | 337+ | In `.tickets/done/` + DD-457 |
| In Progress | 0 | — |
| Ready | 0 | — |
| Black hole | 28+ | DD-214, DD-315, DD-316, DD-318, DD-361, DD-362, DD-365, DD-367 + backseat fix tickets |
| Blocked | 7 | DD-458–464 (design doc gaps, low/medium priority) |

---

## Open Decisions

| Decision | Options | Needed by |
|----------|---------|-----------|
| Screenshot views for ASPN deck | Which 3 pages to polish? (dashboard, resource provider, portal proposed) | Apr 1 |

---

## Reference Docs

| Document | Location |
|----------|----------|
| Architecture + invariants | `CLAUDE.md` |
| Design system | `design-system/MASTER.md` |
| Ticket board | `.tickets/` |
| Product decisions vault | `~/Desktop/RiskNeutral/Vaults/DiveDispatch/` |
| Test coverage report | `coverage/index.html` |
| YouTube library | `~/Desktop/RiskNeutral/Vaults/Matrix-YouTube/Index.md` |
| Course rules + prerequisites | `Vaults/DiveDispatch/Product/CourseRules.md` |
| Instructor ratios | `Vaults/DiveDispatch/Product/InstructorRatios.md` |
| PADI standards + forms | `Vaults/DiveDispatch/Legal/PADI/` (Instructor Manual 2025, medical, liability, DSD, e-sig) |
| Agency definitions (code) | `src/lib/constants/course-catalog.ts`, `convex/shared/agencies.ts` |
| Medical questionnaire (code) | `src/lib/constants/activity-rules.ts`, `convex/portalSubmission.ts` |
| Waivers + liability (code) | `src/lib/constants/waiver-text.ts` |
| Seed data + fixtures | `convex/seed*.ts`, `e2e/helpers/seed.ts` |
| NotebookLM: PADI standards | Notebook "PADI Forms" |
| NotebookLM: Product + Architecture | Notebook "Product & Architecture" |
| **What am I doing?** | `Vaults/DiveDispatch/Product/WhatAmIDoing.md` |
| Happy-path audit (canonical) | `ultraplan/INDEX.md` |
| Happy-path pointer stub | `Vaults/DiveDispatch/Product/HappyPath.md` (points to ultraplan) |
| 24-hour summaries | `Vaults/DiveDispatch/Summaries/` |
| Failure log (for /distill) | `Vaults/DiveDispatch/raw/Failures/` |
| Lessons learned | `Vaults/DiveDispatch/wiki/Architecture/Lessons.md` |
| Pattern library | `Vaults/DiveDispatch/wiki/PatternLibrary/` |
| Session history | `Vaults/DiveDispatch/raw/Sessions/` |
| GitHub integrations | `Vaults/Matrix-GitHub/Integrations.md` |
| SSI/NAUI/CMAS standards | **NOT INGESTED** — no source docs yet |
